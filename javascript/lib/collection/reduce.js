'use strict';

module.exports = {
    /**
     * Reduce function that sums values
     *
     * @param carry
     * @param value
     * @returns {*}
     */
    sum: function(carry, value) {
        return carry + value;
    },
}